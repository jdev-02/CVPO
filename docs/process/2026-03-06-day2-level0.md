# Day 2 Level 0 Log (2026-03-06)

## Objective
Ship a runnable Level 0 "true hello world" single-image classification path.

## What Was Built
1. Added `SigLIPStage` in `src/cvpo/stages/classification.py`:
   - deterministic backend (default) for zero-cost, local, reproducible runs
   - optional `siglip` backend using `transformers` for real model inference
2. Added Level 0 pipeline builder:
   - `src/cvpo/pipelines/level0.py`
   - deterministic single-stage classification flow
3. Extended CLI:
   - `cvpo --level0-demo --labels "goose,duck,pigeon"`
   - prints structured JSON output
4. Updated `Classification` contract to include full score map.
5. Added tests for:
   - Level 0 output shape
   - Determinism guarantees
   - CLI Level 0 demo output

## Determinism Notes
- Default backend is deterministic and uses no external APIs.
- Same image + same labels => identical score map.
- Real SigLIP backend is optional and explicitly configured.

## Next (Day 3)
- Implement YOLOv8 detection stage wrapper
- Implement SAM2 segmentation wrapper
- Build first connector: detection boxes -> segmentation prompts
