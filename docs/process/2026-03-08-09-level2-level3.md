# Day 4-5 Level 2 + Level 3 Log (2026-03-08 to 2026-03-09)

## Objective
Reach a manual-testable end-to-end workflow:
- Level 2: detect -> segment -> classify
- Level 3: detect -> segment -> classify -> track

## What Was Built
1. Added segmentation-to-classification connector:
   - `src/cvpo/connectors/segmentation_to_classification.py`
2. Extended classification contracts:
   - `ClassificationInputSet` and region-level classification support
3. Added Level 2 pipeline:
   - `src/cvpo/pipelines/level2.py`
4. Added deterministic tracking stage:
   - `src/cvpo/stages/tracking.py` (`ByteTrackStage`)
5. Added full Level 3 CLI workflow demo:
   - `cvpo --level3-demo`
   - processes synthetic moving-object frames and returns track continuity data

## Why This Matters
This is the first full-stack deterministic workflow path that a user can manually run end-to-end from the CLI without any external model downloads.

## Manual Test Commands
- `cvpo --level0-demo --labels "goose,duck,pigeon,crow"`
- `cvpo --level1-demo`
- `cvpo --level2-demo --labels "goose,duck,pigeon,crow"`
- `cvpo --level3-demo`

## Next
- Wire real backends incrementally (YOLO/SAM2/SigLIP/ByteTrack where applicable)
- Add real image/video input runner for manual non-synthetic tests
